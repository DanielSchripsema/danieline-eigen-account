name: Run Tests and Generate Coverage Report

on:
  push:
    branches:
      - main

jobs:
  coverage:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./api 
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Install dependencies
        run: npm install
      - name: Run Tests
        run: npm test
      - name: Jest coverage report
        uses: ArtiomTr/jest-coverage-report-action@v2



  # production-deploy:
  #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
  #   runs-on: ubuntu-latest
  #   needs: testen
  #   steps:
  #     # Add steps to deploy to your production environment (e.g., Azure Kubernetes Service)

  # acceptance-deploy:
  #   if: github.event_name == 'pull_request'
  #   runs-on: ubuntu-latest
  #   needs: testen
  #   steps:
  #     # Add steps to deploy to your acceptance environments (e.g., temporary environments for pull requests)
